# ===========================================
# XRPL Node Monitor — Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env    (Linux/macOS)
#   Copy-Item .env.example .env   (Windows PowerShell)

# ---- Required ----

# PostgreSQL connection string
# Examples:
#   Local:  postgresql://postgres:yourpassword@localhost:5432/xrpl_monitor
#   Docker: postgresql://xrpl:changeme@db:5432/xrpl_monitor
#   Neon:   postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/xrpl_monitor?sslmode=require
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/xrpl_monitor

# Session secret — any random string (used for Express sessions)
SESSION_SECRET=change-me-to-a-random-string

# ---- Optional ----

# Server port (default: 5000)
# PORT=5000

# XRPL node connection (default: localhost with standard rippled ports)
# XRPL_HOST=localhost
# XRPL_WS_PORT=6006
# XRPL_HTTP_PORT=5005
# XRPL_ADMIN_PORT=8080

# LM Studio AI integration (uncomment and set to your LM Studio URL)
# LM_STUDIO_URL=http://localhost:1234

# ---- Docker-specific (used by docker-compose.yml) ----

# These set the database credentials inside the Docker PostgreSQL container.
# If you change these, docker-compose.yml picks them up automatically.
# POSTGRES_USER=xrpl
# POSTGRES_PASSWORD=changeme
# POSTGRES_DB=xrpl_monitor
