# ===========================================
# XRPL Node Monitor — Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env    (Linux/macOS)
#   Copy-Item .env.example .env   (Windows PowerShell)

# ---- Required ----

# PostgreSQL connection string
# Examples:
#   Local:  postgresql://postgres:yourpassword@localhost:5432/xrpl_monitor
#   Neon:   postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/xrpl_monitor?sslmode=require
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/xrpl_monitor

# Session secret — any random string (used for Express sessions)
SESSION_SECRET=change-me-to-a-random-string

# ---- Optional ----

# Server port (default: 5000)
# PORT=5000
